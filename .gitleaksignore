# Triaged secret finding — Clerk DEVELOPMENT-instance keypair committed to
# .env.example in 6d7aca7 "Full ACC360 app" (2026-06-23).
#
# Timeline, established 2026-08-25:
#   - 6d7aca7 (2026-06-23) committed the live dev keypair.
#   - 39159d9 (2026-06-23, same day) replaced it with placeholders, so no
#     checked-out tree has carried the real key since — main included. That is
#     why this file, and not an edit to .env.example, is the fix: the finding
#     lives only in history, which a full-history scan still reaches.
#
# Instance: ins_3FVe6JebWFBWXajTk4KNUYT7dKv (quality-crawdad-53), development,
# 7 users. A read-only probe on 2026-08-25 found the key STILL ACCEPTED
# (HTTP 200).
#
# ⚠️ THE CREDENTIAL IS NOT REVOKED. Rotation requires the Clerk dashboard —
# there is no Backend API path for a secret key to revoke itself — and the
# owning Clerk account is a personal one that is currently inaccessible. What
# has been done instead is to retire the instance from ACC's own systems:
# acc360v2's Preview-scoped Clerk keys now point at pleasing-bulldog-80, the
# instance the newsroom already uses. Revoking the leaked key remains an OPEN
# follow-up, tracked in client-newsroom's MERGE_LOG.md.
#
# These entries therefore mean "known, contained, and tracked" — NOT "harmless".
# History is deliberately not rewritten: this is a shared repo, and a rewrite
# would break every existing clone while the credential stayed live anyway.
6d7aca7d475ef11d8b5766ee7ffa43e8e5285978:.env.example:generic-api-key:13
6d7aca7d475ef11d8b5766ee7ffa43e8e5285978:.env.example:stripe-access-token:14
