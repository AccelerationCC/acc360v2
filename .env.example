# ─── Airtable ───────────────────────────────────────────────────────────────
# Found at airtable.com → Account → API → Personal Access Tokens
AIRTABLE_API_KEY=your_airtable_personal_access_token

# Found in your Airtable base URL: airtable.com/appXXXXXXXXXXXX/...
AIRTABLE_BASE_ID=appXXXXXXXXXXXX

# The exact name of your table inside the base (case-sensitive)
AIRTABLE_TABLE_NAME=ACC TL

# ─── Clerk (Authentication) ──────────────────────────────────────────────────
# Found at dashboard.clerk.com → Your App → API Keys

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-key-here
CLERK_SECRET_KEY=sk_test_your-key-here

# Clerk redirect URLs — keep these as-is for local dev
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# ─── Anthropic (AI Chatbot) ──────────────────────────────────────────────────
# Found at console.anthropic.com → API Keys
 ANTHROPIC_API_KEY=sk-ant-your-key-here
# ─── Logo.dev (Company Logos) ─────────────────────────────────────────────────
# Found at logo.dev → Dashboard → API Keys
NEXT_PUBLIC_LOGO_DEV_TOKEN=pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ─── Vercel Blob (observance images) ─────────────────────────────────────────
# LOCAL ONLY. Do NOT add this to any Vercel environment.
#
# It is a WRITE credential for the Blob store. Nothing in the deployed app
# imports @vercel/blob — the single consumer is
# scripts/upload-observance-images.ts, which a person runs on their own machine
# on demand. If this were set in production, any route could write to Blob
# without anyone having decided it should.
#
# Found at vercel.com → the project → Storage → the Blob store → Tokens.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_XXXXXXXXXXXX_XXXXXXXXXXXXXXXXXXXXXXXXXXXX
